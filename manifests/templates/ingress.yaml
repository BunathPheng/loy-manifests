---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: loy                  # Ingress name
  labels:
    app: loy
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "loy.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: loy               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
